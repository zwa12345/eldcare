# WorkBuddy + CodeBuddy + VS Code + Git 开发环境报告

> 整理日期：2026-09-05 19:46
> 整理人：WorkBuddy（agent）
> 适用对象：老张个人协作开发栈

---

## 1. 全局视图

```
┌──────────────────┐    ┌──────────────────┐    ┌──────────────────┐
│   GitHub Cloud   │◄──►│   本机开发环境   │◄──►│  Tailscale 远端  │
│  zwa12345/eldcare│    │   (Windows)      │    │  if-pi05 等      │
│  + SSH 公钥      │    │                  │    │                  │
└──────────────────┘    └──────────────────┘    └──────────────────┘
        ▲                       ▲                       ▲
        │ git push/pull         │ MCP / curl / shell    │ SSH 密码
        │ (SSH)                 │                       │
        ▼                       ▼                       ▼
   ┌──────────┐  ┌────────────────────┐  ┌──────────────────────┐
   │ VS Code  │  │     WorkBuddy      │  │  HomeTheater/aria2   │
   │ + Cb插件 │  │   (本会话的驱动)   │  │  本地媒体库+下载引擎 │
   └──────────┘  └────────────────────┘  └──────────────────────┘
```

**四层角色**

| 层 | 软件 | 角色 |
|------|------|------|
| AI 内核 | **WorkBuddy**（本会话） | 驱动者：调工具、改文件、写代码 |
| AI 内核 | **CodeBuddy CLI** v2.144.0 | 终端里的另一套入口（共享同一内核） |
| IDE | **VS Code** 1.136.1 + **CodeBuddy 扩展** v4.11.37554360 | 图形界面里的人工补全/对话 |
| 版本 | **Git** 2.55.0 + GitHub `zwa12345/eldcare` | 协同 & 历史 |
| 传输 | **SSH** ed25519 + GitHub 远端密钥 | 推拉 |

---

## 2. 工具版本清单

| 工具 | 版本 | 路径 / 备注 |
|------|------|------------|
| Node.js | v24.14.0 | WorkBuddy 托管 |
| npm | 11.9.0 | |
| Python（系统） | 3.14.7 | `AppData\Local\Programs\Python\Python314\` |
| Python（沙箱默认） | 3.13.14 | `WorkBuddyAI\binaries\python\envs\default\` |
| Python（沙箱引擎） | 3.13.12 | `WorkBuddyAI\binaries\python\versions\` |
| pip | 26.1.2 | 沙箱 default venv |
| Git | 2.55.0.windows.3 | 全局 |
| CodeBuddy CLI | 2.144.0 | 全局 `codebuddy` 命令 |
| VS Code | 1.136.1 | 用户级静默安装 |
| CodeBuddy IDE 扩展 | 4.11.37554360 | `~/.vscode/extensions\` |
| SSH 密钥对 | ed25519 | `~/.ssh/id_ed25519`（生成于 2026-09-03 23:16）|
| Tailscale 客户端 | 在线 | 100.64.0.9（本机）|

---

## 3. Git 配置

```
user.name  = zwa12345
user.email = 714363827@qq.com
```

**已知仓库**

| 仓库 | 路径 | 远程 | 状态 |
|------|------|------|------|
| eldcare（本地 Claw） | `C:\Users\zhangwenan\WorkBuddy\Claw` | `git@github.com:zwa12345/eldcare.git` | ✅ 已同步 |
| if-pi05（树莓派） | `pi@100.64.0.21:/home/pi` | 无 | ⚠️ 远程 Linux，密码登录 |

**远程 eldcare 历史**

```
51b6dbb  gitignore: ignore entire .workbuddy/ dir
d232226  init: workspace bootstrap (gitignore + README)
```

---

## 4. SSH 与 GitHub 凭据

| 项 | 状态 | 备注 |
|------|------|------|
| 本机 SSH 公钥 | ✅ 已上传 | 标题 `zhangwenan@workbuddy (workbuddy)`，finger-print `SHA256:/A23TBTRCBFrhvVwBitRXQyNcDcIMRzzN84MI8UXASU` |
| GitHub PAT（mcp.json） | ✅ 写入 | `ghp_***(ghp_9Fl6...)`，完整 scope（`repo`、`admin:public_key` 等），有效期到 **2027-07-31** |
| 旧 PAT | ⚠️ 仍在对话历史 | 建议主动 Revoke（`ghp_***(ghp_oCMO...)`） |

---

## 5. WorkBuddy MCP（`~/.workbuddy/mcp.json`）

```json
{
  "mcpServers": {
    "github":     { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-github"] },
    "filesystem": { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-filesystem", "C:\\Users\\zhangwenan"] }
  }
}
```

| MCP | 状态 | 说明 |
|------|------|------|
| github | ✅ connected | 已用 `get_me`、`get_file_contents` 验证 |
| filesystem | ⚠️ configured, 待 Trust | 需在连接器管理页点 Trust 启用 |
| agent-mail | ✅ connected | 智能体邮箱 |
| ima | ✅ connected | 腾讯 ima 知识库 |
| tencent-docs | ✅ connected | 腾讯文档 |
| wecom | ✅ connected | 企业微信（workBUDDY1 / 张文安）|

> 注意：MCP 进程可能缓存旧 token。换 PAT 后若 403，重启 WorkBuddy 即可加载新 env。

---

## 6. 协同链路（端到端验证通过）

```
1. WorkBuddy 改文件       → 编辑 Claw/ 下文件
2. git commit + push     → SSH 直推 GitHub（无需代理）
3. VS Code clone / pull  → git@github.com:zwa12345/eldcare.git
4. 在 VS Code 中用 CodeBuddy 扩展补全/对话
5. 修改后 push 回 GitHub → WorkBuddy 这边 git pull 同步
```

**验证记录**

- ✅ `git remote add origin` + force-with-lease push（保留 Claw 本地历史）
- ✅ 在 `vscode-test/` 临时目录 `git clone git@github.com:zwa12345/eldcare.git` 拿到完整 working tree 与 commit 历史
- ✅ SSH 通道走 `~/.ssh/id_ed25519`，HTTPS 走代理 502 失败已确认绕路

---

## 7. 本机其他子系统

### 7.1 HomeTheater（家庭影院）

- 路径：`C:\Users\zhangwenan\HomeTheater\`
- 服务端口：**8090**
- 启动：`start.bat`（带守护）
- 模块：aria2c 1.37.0（下载）+ ffmpeg 7.1（转码）+ FastAPI 后端 + 自研前端
- 媒体库：1 部测试片（白云山喂猫），容量 27MB

### 7.2 剪映专业版

- 版本：11.3.0.14362
- 路径：`AppData\Local\JianyingPro\Apps\11.3.0.14362\JianyingPro.exe`
- 状态：当前**未运行**（守护任务退出/过期）

### 7.3 if-pi05 树莓派

- Tailscale IP：100.64.0.21 / 局域网 192.168.2.116
- 系统：Debian 13 trixie / aarch64
- 凭据：`pi / zwa_7571`（密码）
- SSH：已通，公钥未注入（沙箱权限限制，待手动 `ssh-copy-id`）

---

## 8. Claw 工作区（推荐作为项目主目录）

```
C:\Users\zhangwenan\WorkBuddy\Claw\
├── .git/                       # git 仓库
├── .gitignore                  # 屏蔽 .workbuddy/、node_modules/、__pycache__ 等
├── README.md                   # 工作区说明
└── .workbuddy/
    ├── memory/                 # 每日工作日志
    │   ├── 2026-09-03.md
    │   ├── 2026-09-04.md
    │   └── 2026-09-05.md
    └── tmp/                    # 临时脚本（gitignored）
```

**目录约定**

- `.workbuddy/memory/YYYY-MM-DD.md` —— 每日追加工作日志
- `.workbuddy/skills/` —— 项目级 skills（团队共享）
- `.workbuddy/tmp/` —— 临时脚本，已被屏蔽
- 根目录放通用配置（`.gitignore`、`README.md`、`CI` 等）
- 项目代码：在子目录里 `git init` 各自的子仓库（如需独立版本管理）

---

## 9. 已验证的工作流

| 流程 | 命令 | 状态 |
|------|------|------|
| SSH push Claw → GitHub | `git push origin main` | ✅ |
| SSH clone GitHub → 本机 | `git clone git@github.com:zwa12345/eldcare.git` | ✅ |
| force-with-lease 重写历史 | `git push --force-with-lease` | ✅（已校验内容一致）|
| WorkBuddy 调 GitHub | `mcp__github__get_me / create_or_update_file` | ✅ |
| WorkBuddy 调 REST API 兜底 | curl / `urllib` | ✅（当 MCP 缓存 token 时）|

---

## 10. 待办与建议

### 🔴 必须做

- [ ] 登录 https://github.com/settings/tokens **Revoke 旧 PAT**（`ghp_oCMO...04Uqhan`）
- [ ] 在 WorkBuddy 连接器管理页给 **filesystem MCP** 点 Trust 启用

### 🟡 建议做

- [ ] 在 eldcare 仓库加 `.github/workflows/ci.yml`（Python + Node CI）
- [ ] 填写 `~/.workbuddy/IDENTITY.md`、`USER.md`、`SOUL.md`（固定设定存档）
- [ ] 给 if-pi05 注入 SSH 公钥：`ssh-copy-id pi@100.64.0.21`
- [ ] 在 VS Code 中 clone 一份 eldcare 做 IDE 侧日常开发

### 🟢 长期演进

- [ ] HomeTheater 接入 if-pi05 SMB 挂载，统一媒体库
- [ ] eldcare 多模块化：`eldcare/api`（后端）+ `eldcare/web`（前端）
- [ ] WorkBuddy skills 沉淀（建项目、装服务、调网络等高频流程）

---

## 11. 风险与已知坑

| 风险 | 触发场景 | 缓解 |
|------|---------|------|
| MCP 缓存旧 token | 改 mcp.json 后调 GitHub 403 | 重启 WorkBuddy，或用 curl 走 REST API 兜底 |
| HTTPS push 撞代理 502 | `git push https://...` | 改 SSH remote（已配）|
| `wsl.exe` 沙箱黑名单 | 启动 `code.exe` | 沙箱内无法用 code 命令，需在 IDE/外部终端运行 |
| `reg.exe` 沙箱黑名单 | 查注册表 | 改用 PowerShell `[Environment]::GetEnvironmentVariable` |
| VSCode 扩展包是 gzip 流 | 下载 .vsix | curl 必须带 `--compressed` |
| WorkBuddy 沙箱内启动长进程被回收 | 剪映、aria2 守护 | 挂到后台常驻任务（`run_in_background: true`）|

---

## 12. 一句话总结

> 本机已具备 **AI 协同（WorkBuddy/CodeBuddy）+ 图形 IDE（VS Code）+ 版本控制（git/GitHub SSH）+ 媒体与下载引擎（HomeTheater/aria2）+ 远端节点（if-pi05）** 的完整开发底座；主要短板是身份文件（IDENTITY/USER/SOUL）未填、旧 PAT 待 Revoke、CI 与多模块结构尚未建立。
