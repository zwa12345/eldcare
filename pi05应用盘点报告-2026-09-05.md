# if-pi05 应用系统盘点报告

> 盘点时间：2026-09-05 20:30
> 盘点方式：SSH + REST API
> 节点：if-pi05（Tailscale 100.64.0.21 / 局域网 192.168.2.116）

---

## 1. 节点概况

| 项 | 值 |
|------|------|
| 系统 | Debian 13 trixie / aarch64 |
| 内核 | 6.18.34+rpt-rpi-v8 |
| 内存 | 7.6 Gi（已用 1.6 Gi / 缓存 1.9 Gi） |
| 磁盘 | 58G SD 卡，**已用 13G（23%）** |
| 外接存储 | `/mnt/ztv`（exfat，1.8T 移动硬盘） |
| 运行时长 | 3h31m |
| Tailscale | 在线，节点名 `if-pi05`，本地 IP 100.64.0.21 |

---

## 2. 应用服务总览

| 端口 | 应用 | 状态 | PID |
|------|------|------|------|
| 5001 | **kb_manager 家庭影院**（Flask） | ✅ 活跃 | 1346 |
| 9100 | **nms-agent**（节点监控） | ❌ **未在 listen** | 1575 进程在但未 bind 端口 |
| 8123 | **mcp_edge_server**（RPi 边的 MCP） | ❌ **未在 listen** | 进程不在 ps 中 |
| 22 | sshd | ✅ | systemd |
| 41641 | tailscaled | ✅ | systemd |

> 关键问题：**nms-agent 和 mcp_edge_server 都声明自己是 systemd 服务（nms-agent.service 在 systemd 里 active），但实际端口都没监听。** 可能是启动后崩了但 systemd 没正确重启。

---

## 3. 应用一：Hermes Agent（AI 网关 + 多通道代理）

### 3.1 部署位置

```
/home/pi/.hermes/hermes-agent/   # 安装根目录
/home/pi/.hermes/hermes-agent/venv/  # Python 隔离环境
/home/pi/.hermes/cron/jobs.json  # 定时任务
```

### 3.2 能力

`hermes` CLI 是一个**功能极强的 AI Agent 框架**（v0.18.0），子命令 60+：

| 类别 | 子命令 |
|------|--------|
| 模型 | `chat / model / moa / fallback / proxy` |
| 集成 | `whatsapp / slack / discord / weixin` |
| 调度 | `cron / webhook / gateway / proxy` |
| 自动化 | `kanban / project / hooks / pets / journey / learning` |
| 记忆 | `memory / memory-graph / sessions / insights` |
| 工具 | `tools / computer-use / mcp / desktop / gui` |
| 扩展 | `skills / bundles / plugins / secrets` |

### 3.3 当前活跃：Hermes Gateway

```
PID 1345
CGroup: user.slice/user-1000.slice/user@1000.service/app.slice/hermes-gateway.service
启动: 2026-09-05 16:42:35 CST
已运行: 3h 47min
CPU 累计: 4min 30s（健康）
```

### 3.4 依赖

```
anthropic 0.87.0 / openai / google-api-python-client / boto3
fastapi 0.133.1 / aiohttp 3.14.1
agent-client-protocol 0.9.0
edge-tts 7.2.7  # TTS
croniter 6.0.0  # 调度
... 几十个
```

### 3.5 定时任务（`/home/pi/.hermes/cron/jobs.json`）

发现至少 1 个 job：

| Job ID | 名称 | 频率 | 已运行 | 模型 |
|--------|------|------|--------|------|
| `519a7430ce2b` | **auto-movie-download** | 每 30 分钟 | 141 次 | minimax-cn / MiniMax-M2 |

这个 job 直接驱动 if-pi05 的电影自动下载流程（详见应用二）。

---

## 4. 应用二：kb_manager 家庭影院（Flask 应用）

### 4.1 部署位置

```
/home/pi/knowledgebase_project/kb_manager/        # Django 项目壳
/home/pi/knowledgebase_project/kb_manager/home_theater/  # 实际 Flask 应用
/home/pi/knowledgebase_project/kb_manager/home_theater/venv/  # 独立 venv
/home/pi/knowledgebase_project/kb_manager/home_theater/movies -> /mnt/ztv/movies  # 软链
```

### 4.2 主程序：`app.py`（13.5 KB Flask）

**核心路由（30+ endpoints）**

| 类别 | 路由 |
|------|------|
| 页面 | `/`、`/player/<id>`、`/search`、`/category/<slug>` |
| CRUD 电影 | `GET/POST/PUT/DELETE /api/movie[/<id>]` |
| 浏览 | `/api/movies[/stats/recent/top-rated/random/recommend]` |
| 分类 | `/api/categories`、`/api/crawl/local` |
| 搜索 | `/api/search`、`/api/search/history`（GET/DELETE） |
| 收藏 | `POST /api/movie/<id>/favorite` |
| 播放 | `/api/video/<id>/stream`（支持 Range）、`/api/video/<id>/subtitle` |
| 爬虫 | `/api/crawl/douban`、`/api/crawl/douban/all`、`/api/crawl/bt` |
| 进度 | `/api/watch/position/<id>` |
| 反代 | `/api/proxy/poster`（防 TMDB 链接防盗链） |

### 4.3 API 实测（HTTP 200 全通）

```
GET /api/movies         → {"code":0,"data":{"items":[...]},"message":""}
GET /api/movies/stats   → 200
GET /api/movies/recent  → 200
GET /api/categories     → 200
GET /api/crawl/douban   → 200
```

样例数据：Kelly Macdonald / Billy Connolly / Emma Thompson 的影片（含 TMDB 海报 URL），说明已成功接入 TMDB 元数据。

### 4.4 数据库：`movies.db`（118 KB SQLite）

- `home_theater/movies.db`（主库）
- `home_theater/movies.db.bak_20260901_200002`（备份）

> 注：远端未装 `sqlite3` CLI，未能直接 COUNT；下次可以用 Python sqlite3 模块远程查询统计。

### 4.5 关联脚本

| 脚本 | 作用 |
|------|------|
| `database.py` | ORM / 数据访问层 |
| `crawler.py` | 通用爬虫框架 |
| `auto_download.py` | 自动下载调度（被 Hermes cron 调用） |
| `bilibili_crawler.py` | B 站爬虫 |
| `download_movie.py` / `multi_download.py` / `download_subtitles.py` | 下载工具集 |
| `mt_http_server.py` | 多线程 HTTP 文件服务（基于 aiohttp） |
| `relay_download.py` / `relay_lt.py` | 中继下载（libtorrent） |
| `security.py` | 安全相关 |
| `start.sh` | 启动脚本（默认端口 5001） |

### 4.6 自动下载流程（已运行 141 次）

```
[每 30 分钟触发] (由 Hermes cron job 519a7430ce2b)
   ↓
1. python3 auto_download.py
2. 检查中继服务器 (47.253.53.228) 磁盘 / HTTP 健康
3. 爬取 YTS 热门电影（约 6 部）
4. 过滤已下载
5. 派发到中继服务器用 libtorrent 下载
6. 通过 HTTP 单线程下载到本机 /mnt/ztv/movies
7. 录入 movies.db
8. 输出回 Hermes 当前聊天
```

最近一次（20:15:52）结果：6 个候选全部因"已下载"被跳过，数据库已饱和。

### 4.7 服务管理

- **systemd user service**：`/home/pi/.config/systemd/user/home-theater.service`（已 enabled）
- 与 Hermes gateway 是平级用户服务

---

## 5. 应用三：nms-agent（节点监控上报）

### 5.1 部署位置

```
/opt/nms/agent/agent.py    # 单文件 18 KB
/etc/systemd/system/nms-agent.service  # system 级 service
```

### 5.2 设计

```
监听: 127.0.0.1:9100
用途: Tailscale 节点监控指标上报
Header: "仅 Tailscale 内网可达，公网不可访问"
依赖: psutil（fallback /proc）
```

### 5.3 当前状态：❌ 异常

| 项 | 实测 |
|------|------|
| systemd 状态 | ✅ active (running)，启动 3h47min |
| 进程 PID 1575 | ✅ 存在 |
| 端口 9100 | ❌ 不在 listen（ss 查询无结果） |
| HTTP 探测 | 全部 000（连接失败） |

**说明**：进程在，但没成功 bind 9100 端口。从 agent.py 头部看应该有 `HTTPServer(('127.0.0.1', 9100), Handler)`——可能是启动顺序里 9100 被占、或者脚本里 try/except 把 bind 异常吞了。建议远程登录看 stderr 或重启服务。

---

## 6. 应用四：mcp_edge_server（RPi 边 MCP）

### 6.1 设计

```
/home/pi/mcp_edge_server.py    # 13.6 KB
端口: 0.0.0.0:8123
框架: fastmcp ("RPi-Edge-Monitor" v1.0)
Auth: Bearer Token Hermes@Edge2026
```

### 6.2 已注册的 MCP 工具

| 工具 | 作用 |
|------|------|
| `get_system_uptime()` | 运行时长（天数/小时/分钟） |
| `get_cpu_load()` | CPU 核心/使用率/1-15min 负载 |
| `get_memory_status()` | 内存 + Swap（部分） |

### 6.3 当前状态：❌ 异常

| 项 | 实测 |
|------|------|
| 进程 | 不在 ps 中 |
| 端口 8123 | ❌ 全部 000 |

脚本没有自启动（不像 nms-agent 有 systemd unit），只在 `run.log` 里记录过历史启动：
```
Edge MCP Server start http://0.0.0.0:8123/mcp
Auth Bearer Token: Hermes@Edge2026
```

---

## 7. 其它脚本与凭据

| 位置 | 备注 |
|------|------|
| `/home/pi/bt_download.py` | 本地 libtorrent 单文件下载器 |
| `/home/pi/relay_bt_download.py` | 中继服务器用 libtorrent 下载器 |
| `/home/pi/.secrets/relay_key.pem` / `.pub` | SSH 公私钥对（relay 用） |
| `/home/pi/.secrets/relay_pass.enc` | 中继登录密码（加密存储） |
| `/home/pi/sysctl` | 内核参数 |
| `/home/pi/run.log` | mcp_edge_server 历史启动日志 |

---

## 8. Tailscale 节点矩阵

```
100.64.0.21  if-pi05                   active   linux    ← 本次盘点节点
100.64.0.20  debian12                  offline (2d)
100.64.0.16  desktop-957mrug           offline (1d)
100.64.0.25  desktop-im600ij           active   windows
100.64.0.4   desktop-iq1p273           offline (2d)
100.64.0.9   desktop-pabs4gn           offline (1d) ← 这就是老张的本机
100.64.0.15  henry-zhang-ydkuoqmw      offline (2d)
100.64.0.12  if-pi1                    offline (1d)
```

---

## 9. 问题与修复建议

### 🔴 必须修

1. **nms-agent 端口未 bind**：进程在但 9100 没人监听
   - 排查：`systemctl status nms-agent` 看 stderr；`journalctl -u nms-agent -n 50`
   - 修复：远程登录后 `systemctl restart nms-agent`；检查 9100 是否被占；agent.py 启动加 try/except 日志
2. **mcp_edge_server 没运行**：缺 systemd unit，shell 进程死就退
   - 修复：写一个 systemd unit，挂到 `home-theater.service` 同级别
   - 优势：可被 WorkBuddy 经由 mcp.json 远程调用，自动盘点 pi05 状态

### 🟡 建议做

3. **Hermes cron jobs.json 单 job 触发 auto_download**：每次 6 候选都"已下载"跳过，建议
   - 加随机 / 按评分选取，避免总是同一批
   - 加同集多版本去重（已实现？）
   - 加密/解密的 `relay_pass.enc` 完整流程说明缺
4. **kb_manager 的 kb_manager 子目录**：`/home/pi/knowledgebase_project/kb_manager/kb_manager/` 是 Django 项目主模块，但目前没看到 web 进程（说明只用了 home_theater app，Django 这层未启）
5. **kb_manager/manage.py + db.sqlite3** 0 字节：Django ORM 项目壳空置
6. **movies.db 备份策略**：现仅一份手动备份 `bak_20260901_200002`，应做定期快照

### 🟢 长期演进

7. **合并重复的家庭影院**：if-pi05 (Flask + SQLite) 与本机 HomeTheater (FastAPI + 内存) 能否数据互通
8. **统一指标采集**：nms-agent + mcp_edge_server 应合二为一（重叠严重）
9. **Hermes 容器化**：`hermes-agent/docker/` 已存在，可以从源码 venv 迁移到容器化部署

---

## 10. 一句话总结

> if-pi05 是**老张家的私有云家庭网关**：跑着 1 套 AI Agent 框架（Hermes）+ 1 套家庭影院服务（kb_manager 5001，已稳定运行 141 次自动下载）+ 2 套监控工具（nms-agent 9100 + mcp_edge 8123，**均异常未运行**）；主要短板是 2 个监控服务没起来，以及两个家庭影院系统分处本机与 if-pi05 还未做数据互通。
